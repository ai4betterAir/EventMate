const createModal=document.getElementById('createModal');const joinModal=document.getElementById('joinModal');const toast=document.getElementById('toast');document.getElementById('year').textContent=new Date().getFullYear();function openModal(modal){modal.hidden=false;document.body.style.overflow='hidden'}function closeModal(modal){modal.hidden=true;document.body.style.overflow=''}function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)}document.querySelectorAll('[data-action="create"]').forEach(btn=>btn.addEventListener('click',()=>openModal(createModal)));document.querySelectorAll('[data-action="join"]').forEach(btn=>btn.addEventListener('click',()=>openModal(joinModal)));document.querySelectorAll('[data-close]').forEach(btn=>btn.addEventListener('click',()=>closeModal(btn.closest('.modal-backdrop'))));[createModal,joinModal].forEach(modal=>modal.addEventListener('click',e=>{if(e.target===modal)closeModal(modal)}));document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(!createModal.hidden)closeModal(createModal);if(!joinModal.hidden)closeModal(joinModal)}});document.getElementById('eventForm').addEventListener('submit',e=>{e.preventDefault();const form=new FormData(e.target);const event={id:`EVT-${Math.floor(1000+Math.random()*9000)}`,name:form.get('name'),type:form.get('type'),date:form.get('date'),time:form.get('time'),location:form.get('location'),capacity:form.get('capacity'),cost:form.get('cost'),createdAt:new Date().toISOString()};const events=JSON.parse(localStorage.getItem('eventmate-events')||'[]');events.push(event);localStorage.setItem('eventmate-events',JSON.stringify(events));closeModal(createModal);e.target.reset();showToast(`${event.name} created · Code ${event.id}`)});document.getElementById('joinForm').addEventListener('submit',e=>{e.preventDefault();const code=new FormData(e.target).get('code').trim().toUpperCase();const events=JSON.parse(localStorage.getItem('eventmate-events')||'[]');const found=events.find(event=>event.id===code);closeModal(joinModal);e.target.reset();showToast(found?`Found: ${found.name}`:`No saved demo event found for ${code}`)});